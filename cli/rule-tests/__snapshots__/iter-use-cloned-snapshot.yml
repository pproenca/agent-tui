id: iter-use-cloned
snapshots:
  iter.map(|x| x.clone());:
    fixed: iter.cloned();
    labels:
    - source: iter.map(|x| x.clone())
      style: primary
      start: 0
      end: 23
  self.data.iter().map(|x| x.clone());:
    fixed: self.data.iter().cloned();
    labels:
    - source: self.data.iter().map(|x| x.clone())
      style: primary
      start: 0
      end: 35
  vec.iter().map(|item| item.clone());:
    fixed: vec.iter().cloned();
    labels:
    - source: vec.iter().map(|item| item.clone())
      style: primary
      start: 0
      end: 35
