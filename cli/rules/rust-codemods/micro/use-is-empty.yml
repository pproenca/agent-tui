id: micro-use-is-empty
language: rust
severity: hint
message: "Use is_empty() instead of len() == 0"
note: |
  is_empty() is more idiomatic and expressive than len() == 0.
  It clearly communicates the intent to check for emptiness.
rule:
  pattern: $EXPR.len() == 0
fix: $EXPR.is_empty()
