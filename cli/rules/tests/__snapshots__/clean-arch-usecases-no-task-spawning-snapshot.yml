id: clean-arch-usecases-no-task-spawning
snapshots:
  ? |
    fn run() {
        std::thread::spawn(|| {});
    }
  : labels:
    - source: std::thread::spawn(|| {})
      style: primary
      start: 15
      end: 40
  ? |
    fn run() {
        tokio::spawn(async move {});
    }
  : labels:
    - source: tokio::spawn(async move {})
      style: primary
      start: 15
      end: 42
  ? |
    fn run() {
        tokio::task::spawn_blocking(|| {});
    }
  : labels:
    - source: tokio::task::spawn_blocking(|| {})
      style: primary
      start: 15
      end: 49
