id: iter-use-any
snapshots:
  items.filter(|item| item.is_valid()).next().is_some();:
    fixed: items.any(|item| item.is_valid());
    labels:
    - source: items.filter(|item| item.is_valid()).next().is_some()
      style: primary
      start: 0
      end: 53
  iter.filter(|x| x > 0).next().is_some();:
    fixed: iter.any(|x| x > 0);
    labels:
    - source: iter.filter(|x| x > 0).next().is_some()
      style: primary
      start: 0
      end: 39
  vec.iter().filter(predicate).next().is_some();:
    fixed: vec.iter().any(predicate);
    labels:
    - source: vec.iter().filter(predicate).next().is_some()
      style: primary
      start: 0
      end: 45
