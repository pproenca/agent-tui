id: iter-use-cloned
snapshots:
  items.iter().map(|item| item.clone());:
    fixed: items.iter().cloned();
    labels:
    - source: items.iter().map(|item| item.clone())
      style: primary
      start: 0
      end: 37
  iter.map(|x| x.clone());:
    fixed: iter.cloned();
    labels:
    - source: iter.map(|x| x.clone())
      style: primary
      start: 0
      end: 23
  vec.iter().map(|v| v.clone()).collect();:
    fixed: vec.iter().cloned().collect();
    labels:
    - source: vec.iter().map(|v| v.clone())
      style: primary
      start: 0
      end: 29
