id: micro-use-is-empty
snapshots:
  if items.len() == 0 { return; }:
    fixed: if items.is_empty() { return; }
    labels:
    - source: items.len() == 0
      style: primary
      start: 3
      end: 19
  self.buffer.len() == 0;:
    fixed: self.buffer.is_empty();
    labels:
    - source: self.buffer.len() == 0
      style: primary
      start: 0
      end: 22
  vec.len() == 0;:
    fixed: vec.is_empty();
    labels:
    - source: vec.len() == 0
      style: primary
      start: 0
      end: 14
