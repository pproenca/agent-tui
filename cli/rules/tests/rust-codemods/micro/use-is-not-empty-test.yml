id: micro-use-is-not-empty
valid:
  - "!vec.is_empty();"
  - "if !items.is_empty() { process(); }"
  - "s.len() >= 1;"
invalid:
  - "vec.len() != 0;"
  - "items.len() > 0;"
  - "if self.buffer.len() != 0 { process(); }"
  - "if self.buffer.len() > 0 { process(); }"
