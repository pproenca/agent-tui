# Snapshot Command Tests

Tests for taking screen snapshots. These tests require an active session.

Note: These tests expect a session to be available. In E2E testing,
the daemon auto-starts and we can spawn sessions first.
