# Click Command Tests

Tests for clicking elements.

Note: Click commands require an active session with detected elements.
Basic argument parsing is tested here; full E2E interaction requires
a running session with elements.
