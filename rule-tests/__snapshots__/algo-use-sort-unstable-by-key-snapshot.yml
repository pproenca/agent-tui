id: algo-use-sort-unstable-by-key
snapshots:
  items.sort_unstable_by(|a, b| a.key.cmp(&b.key));:
    fixed: items.sort_unstable_by_key(|x| x.key);
    labels:
    - source: items.sort_unstable_by(|a, b| a.key.cmp(&b.key))
      style: primary
      start: 0
      end: 48
  self.data.sort_unstable_by(|a, b| a.name.cmp(&b.name));:
    fixed: self.data.sort_unstable_by_key(|x| x.name);
    labels:
    - source: self.data.sort_unstable_by(|a, b| a.name.cmp(&b.name))
      style: primary
      start: 0
      end: 54
  vec.sort_unstable_by(|a, b| a.field.cmp(&b.field));:
    fixed: vec.sort_unstable_by_key(|x| x.field);
    labels:
    - source: vec.sort_unstable_by(|a, b| a.field.cmp(&b.field))
      style: primary
      start: 0
      end: 50
