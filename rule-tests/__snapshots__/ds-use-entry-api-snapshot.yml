id: ds-use-entry-api
snapshots:
  if !cache.contains_key(&k) { cache.insert(k, compute()); }:
    labels:
    - source: if !cache.contains_key(&k) { cache.insert(k, compute()); }
      style: primary
      start: 0
      end: 58
  if !map.contains_key(&key) { map.insert(key, value); }:
    labels:
    - source: if !map.contains_key(&key) { map.insert(key, value); }
      style: primary
      start: 0
      end: 54
