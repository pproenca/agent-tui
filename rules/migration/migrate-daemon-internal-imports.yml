id: migrate-daemon-domain
language: rust
rule:
  pattern: use crate::domain::$REST;
fix: use crate::daemon::domain::$REST;

---
id: migrate-daemon-adapters
language: rust
rule:
  pattern: use crate::adapters::$REST;
fix: use crate::daemon::adapters::$REST;

---
id: migrate-daemon-handlers
language: rust
rule:
  pattern: use crate::handlers::$REST;
fix: use crate::daemon::handlers::$REST;

---
id: migrate-daemon-usecases
language: rust
rule:
  pattern: use crate::usecases::$REST;
fix: use crate::daemon::usecases::$REST;

---
id: migrate-daemon-wait
language: rust
rule:
  pattern: use crate::wait::$REST;
fix: use crate::daemon::wait::$REST;

---
id: migrate-daemon-ansi-keys
language: rust
rule:
  pattern: use crate::ansi_keys::$REST;
fix: use crate::daemon::ansi_keys::$REST;

---
id: migrate-daemon-select-helpers
language: rust
rule:
  pattern: use crate::select_helpers::$REST;
fix: use crate::daemon::select_helpers::$REST;

---
id: migrate-daemon-repository
language: rust
rule:
  pattern: use crate::repository::$REST;
fix: use crate::daemon::repository::$REST;

---
id: migrate-daemon-terminal-state
language: rust
rule:
  pattern: use crate::terminal_state::$REST;
fix: use crate::daemon::terminal_state::$REST;

---
id: migrate-daemon-lock-helpers
language: rust
rule:
  pattern: use crate::lock_helpers::$REST;
fix: use crate::daemon::lock_helpers::$REST;

---
id: migrate-daemon-transport
language: rust
rule:
  pattern: use crate::transport::$REST;
fix: use crate::daemon::transport::$REST;

---
id: migrate-daemon-test-support
language: rust
rule:
  pattern: use crate::test_support::$REST;
fix: use crate::daemon::test_support::$REST;

---
id: migrate-daemon-pty-session
language: rust
rule:
  pattern: use crate::pty_session::$REST;
fix: use crate::daemon::pty_session::$REST;

---
id: migrate-daemon-error
language: rust
rule:
  pattern: use crate::error::$REST;
fix: use crate::daemon::error::$REST;

---
id: migrate-daemon-config
language: rust
rule:
  pattern: use crate::config::$REST;
fix: use crate::daemon::config::$REST;

---
id: migrate-daemon-server
language: rust
rule:
  pattern: use crate::server::$REST;
fix: use crate::daemon::server::$REST;

---
id: migrate-daemon-session
language: rust
rule:
  pattern: use crate::session::$REST;
fix: use crate::daemon::session::$REST;

---
id: migrate-daemon-router
language: rust
rule:
  pattern: use crate::router::$REST;
fix: use crate::daemon::router::$REST;

---
id: migrate-daemon-signal-handler
language: rust
rule:
  pattern: use crate::signal_handler::$REST;
fix: use crate::daemon::signal_handler::$REST;

---
id: migrate-daemon-sleeper
language: rust
rule:
  pattern: use crate::sleeper::$REST;
fix: use crate::daemon::sleeper::$REST;

---
id: migrate-daemon-metrics
language: rust
rule:
  pattern: use crate::metrics::$REST;
fix: use crate::daemon::metrics::$REST;

---
id: migrate-daemon-usecase-container
language: rust
rule:
  pattern: use crate::usecase_container::$REST;
fix: use crate::daemon::usecase_container::$REST;

---
id: migrate-daemon-file-lock
language: rust
rule:
  pattern: use crate::file_lock::$REST;
fix: use crate::daemon::file_lock::$REST;
