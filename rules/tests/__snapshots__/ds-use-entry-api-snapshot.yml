id: ds-use-entry-api
snapshots:
  if !map.contains_key(&key) { map.insert(key, value); }:
    labels:
    - source: if !map.contains_key(&key) { map.insert(key, value); }
      style: primary
      start: 0
      end: 54
  if !self.cache.contains_key(&id) { self.cache.insert(id, data); }:
    labels:
    - source: if !self.cache.contains_key(&id) { self.cache.insert(id, data); }
      style: primary
      start: 0
      end: 65
