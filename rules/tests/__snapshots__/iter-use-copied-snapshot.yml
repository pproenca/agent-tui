id: iter-use-copied
snapshots:
  items.iter().map(|item| *item);:
    fixed: items.iter().copied();
    labels:
    - source: items.iter().map(|item| *item)
      style: primary
      start: 0
      end: 30
  iter.map(|x| *x);:
    fixed: iter.copied();
    labels:
    - source: iter.map(|x| *x)
      style: primary
      start: 0
      end: 16
  vec.iter().map(|v| *v).collect();:
    fixed: vec.iter().copied().collect();
    labels:
    - source: vec.iter().map(|v| *v)
      style: primary
      start: 0
      end: 22
