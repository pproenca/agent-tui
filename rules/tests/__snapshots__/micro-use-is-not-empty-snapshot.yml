id: micro-use-is-not-empty
snapshots:
  if self.buffer.len() != 0 { process(); }:
    fixed: if !self.buffer.is_empty() { process(); }
    labels:
    - source: self.buffer.len() != 0
      style: primary
      start: 3
      end: 25
  if self.buffer.len() > 0 { process(); }:
    fixed: if !self.buffer.is_empty() { process(); }
    labels:
    - source: self.buffer.len() > 0
      style: primary
      start: 3
      end: 24
  items.len() > 0;:
    fixed: '!items.is_empty();'
    labels:
    - source: items.len() > 0
      style: primary
      start: 0
      end: 15
  vec.len() != 0;:
    fixed: '!vec.is_empty();'
    labels:
    - source: vec.len() != 0
      style: primary
      start: 0
      end: 14
