id: own-accept-slice-not-vec
valid:
  - |
    fn process_items(items: &[i32]) {
        for item in items { println!("{}", item); }
    }
  - |
    fn process_strings(items: &[String]) {
        for item in items { println!("{}", item); }
    }
  - |
    fn take_ownership(items: Vec<i32>) {
        drop(items);
    }
invalid:
  - |
    fn process_items(items: &Vec<i32>) {
        for item in items { println!("{}", item); }
    }
  - |
    fn process_strings(items: &Vec<String>) {
        for item in items { println!("{}", item); }
    }
