{
  "name": "agent-tui-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "test": "bun test",
    "dev": "bun run build.ts && bun --hot server.ts",
    "serve": "bun server.ts"
  },
  "dependencies": {
    "lucide": "^0.563.0",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  }
}
